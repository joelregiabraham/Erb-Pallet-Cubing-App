<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for CT50 Scanner, Database, and File Export -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Erb Pallet Cubing"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ErbPalletCubing"
        tools:targetApi="31">

        <!-- LoginActivity - LAUNCHER -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Scanner Intent Filter -->
            <intent-filter>
                <action android:name="com.erb.erbpalletcubing.SCAN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- TrailerActivity - Phase 2 -->
        <activity
            android:name=".TrailerActivity"
            android:screenOrientation="portrait" />

        <!-- ProHeaderActivity - Phase 3 -->
        <activity
            android:name=".ProHeaderActivity"
            android:screenOrientation="portrait" />

        <!-- PalletDetailActivity - Phase 4 -->
        <activity
            android:name=".PalletDetailActivity"
            android:screenOrientation="portrait" />

        <!-- SummaryActivity - Phase 5 -->
        <activity
            android:name=".SummaryActivity"
            android:screenOrientation="portrait" />

        <!-- FileProvider for secure file sharing (Phase 5 - CSV Export) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.erb.erbpalletcubing.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>